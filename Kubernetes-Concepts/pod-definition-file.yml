apiVersion: v1
kind: Pod

metadata:
  name: myapp-pod
  labels: 
    app: myapp
    type: front-end
spec:
  containers:
    - name: nginx-container-1
      image: nginx
      resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
      

#to start 
#kubectl create -f pod-definition.yml      