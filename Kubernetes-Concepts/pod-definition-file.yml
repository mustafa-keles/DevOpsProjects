apiVersion: v1
kind: Pod

metadata:
  name: myapp-pod
  labels: 
    app: myapp
    type: front-end
spec:
  containers:
    - name: nginx-container-1
      image: nginx
      

#to start 
#kubectl create -f pod-definition.yml      